# Story 07B.1 — Lista de agentes + Novo agente (form MVP)

**Status:** Ready for Review
**Phase:** 07B (UI essencial)
**Owner agent:** `@dev` (Neo)
**Story type:** UI + server actions
**Estimated effort:** 60-90 min (real: ~75 min)
**Validation:** Vinni dispensou `@po *validate-story-draft` em 2026-04-19 (pattern 07A). Story aprovada direto pra `@dev`.
**Build status:** `pnpm -F web type-check` ✅ PASS | `pnpm -F web lint` ✅ PASS (só meus arquivos) | `pnpm -F web build` ✅ PASS

## Context

Substituir os dois placeholders existentes (`/agents` e `/agents/new`) por versões funcionais: (a) lista em grid mostrando os agentes da org, (b) form MVP de criação que cria agente em `DRAFT` e redireciona pro detalhe.

Esta story **não** implementa o detalhe do agente — isso é 07B.2. Aqui foco é: chegar no detalhe com um agente existente.

**Spec de referência:** `docs/phase-07/ui-spec-07b-agent-detail.md` — seção 6 (Tela de lista) e subseção 6.4 (form MVP).

## Acceptance Criteria

### Lista de agentes (`/app/[orgSlug]/agents`)

1. Substituir `ComingSoon` pela lista real respeitando `PageHeader` e botão "+ Novo agente" já existentes no header da rota.
2. Buscar agentes da org ativa via `getAgentsByOrg(organizationId)` (server component, `cache()`).
3. Renderizar grid responsivo: `grid-cols-1 md:grid-cols-2 xl:grid-cols-3 gap-4`.
4. Cada card mostra: avatar 48px, nome (bold, truncate), role (muted, truncate), status badge, nome do modelo (texto curto tipo "GPT-4.1 mini"), vínculo WhatsApp (ou "Sem WhatsApp" se null), versão (ex "v3"), dropdown de ações `⋯`.
5. Click no card (não no dropdown) navega pra `/agents/[agentId]` (aba Identidade — 07B.3 entrega conteúdo).
6. Dropdown `⋯` (usando `DropdownMenu` do shadcn) com itens:
   - Ver detalhe
   - Duplicar (cria cópia em DRAFT com `" (cópia)"` no nome → redireciona pro detalhe da cópia)
   - Ativar / Pausar (dependendo do status atual)
   - Arquivar (abre `AlertDialog` de confirmação)
7. Status badges com cores:
   - `DRAFT` → neutro/cinza
   - `ACTIVE` → verde
   - `PAUSED` → amarelo
   - `ARCHIVED` → vermelho suave
8. Empty state quando sem agentes: usar `ComingSoon` existente com texto atualizado + botão "Criar agente" centralizado (leva pra `/agents/new`):
   - Título: "Seus agentes aparecerão aqui"
   - Descrição: "Crie seu primeiro agente comercial com o formulário rápido."
9. Arquivados **não aparecem** na lista por default. (Opcional 07B: não implementar toggle "ver arquivados" — fica pra 07C.)

### Form MVP (`/app/[orgSlug]/agents/new`)

10. Substituir `ComingSoon` pelo form de criação.
11. Card centralizado `max-w-lg` com `PageHeader` atualizado:
    - Título: "Novo agente"
    - Subtítulo: "Configure o básico — você edita os detalhes depois"
12. Campos:
    - **Nome** (`name`) — `Input`, obrigatório, 2-80 chars
    - **Função** (`role`) — `Input`, opcional, max 80 chars, placeholder "Atendimento comercial"
    - **Modelo** (`model`) — `Select`, obrigatório, default `openai/gpt-4.1-mini`
13. Select de modelo usa lista curada definida em novo arquivo `packages/ai/src/mastra/models.ts` (ou `packages/ai/src/models.ts`), export nomeado `SUPPORTED_MODELS` com estrutura:
    ```typescript
    export const SUPPORTED_MODELS = [
      { id: 'openai/gpt-4.1-mini', label: 'GPT-4.1 mini', provider: 'openai', isDefault: true },
      { id: 'openai/gpt-4.1', label: 'GPT-4.1', provider: 'openai' },
      { id: 'openai/gpt-4o-mini', label: 'GPT-4o mini', provider: 'openai' },
      { id: 'anthropic/claude-haiku-4-5', label: 'Claude Haiku 4.5', provider: 'anthropic' },
      { id: 'anthropic/claude-sonnet-4-6', label: 'Claude Sonnet 4.6', provider: 'anthropic' },
    ];
    ```
14. Select agrupa por provider (OpenAI / Anthropic) visualmente.
15. Botões do rodapé do form:
    - **Cancelar** → `Link` pra `/app/[orgSlug]/agents`
    - **Criar agente** → submit. Disabled se `!form.formState.isValid || isSubmitting`. Spinner + "Criando..." durante submit.
16. Server action `createAgent({ name, role, model, organizationId })`:
    - Valida com zod.
    - Cria em `status: DRAFT`, `version: 1`, defaults pra persona/business/conversationStyle (objetos `{}`), `enabledTools: []`, `knowledgeDocIds: []`, `whatsappInstanceId: null`.
    - RLS: confirma que user atual tem role `admin` ou `owner` na org (retorna 403 caso contrário).
    - Retorna `{ agentId }`, client redireciona via `router.push('/app/[orgSlug]/agents/' + agentId)`.
17. Toast de sucesso após criar: "Agente criado".

### Ações secundárias

18. Server action `duplicateAgent({ agentId })` — cria cópia idêntica com:
    - `name: originalName + ' (cópia)'`
    - `status: 'DRAFT'`
    - `version: 1`
    - `whatsappInstanceId: null` (não duplica vínculo)
    - `publishedAt: null`
19. Server action `archiveAgent({ agentId })` / `unarchiveAgent({ agentId })` — altera só `status`. Arquivar libera o vínculo WhatsApp (`whatsappInstanceId: null`).
20. Server action `toggleAgentStatus({ agentId, to: 'ACTIVE' | 'PAUSED' })` — valida pré-condições antes de ativar (nome, role e modelo preenchidos); se faltar campo, retorna erro com mensagem clara pro toast.

## Dependencies

- **Blocked by:** Phase 07A done (schema `agent` + `agent_version` existe)
- **Blocks:** 07B.2 (detalhe precisa de agentes existentes pra render)

## File List (esperado)

**Novos (9 arquivos):**
- `packages/ai/src/mastra/models.ts` — `SUPPORTED_MODELS` + helpers (`findModel`, `getModelLabel`, `getModelsByProvider`, `getDefaultModelForProvider`, `DEFAULT_MODEL_ID`)
- `packages/ai/models.ts` — subpath export pra uso em client components (browser-safe, não puxa Mastra/pg)
- `apps/web/modules/saas/agents/lib/schemas.ts` — zod schemas compartilhados (status, gender, model, create, archive, toggle)
- `apps/web/modules/saas/agents/lib/server.ts` — `getAgentsByOrg(organizationId)` + `getAgentById(agentId)` com `cache()` do React
- `apps/web/modules/saas/agents/lib/actions.ts` — `createAgentAction`, `duplicateAgentAction`, `archiveAgentAction`, `unarchiveAgentAction`, `toggleAgentStatusAction`
- `apps/web/modules/saas/agents/components/AgentStatusBadge.tsx` — badge com cores mapeadas pro `Badge` do boilerplate (status `info/success/warning/error`)
- `apps/web/modules/saas/agents/components/AgentCard.tsx` — card individual com avatar fallback, stats e status
- `apps/web/modules/saas/agents/components/AgentCardActions.tsx` — dropdown de ações client (ver detalhe, duplicar, ativar/pausar, arquivar com AlertDialog)
- `apps/web/modules/saas/agents/components/AgentsList.tsx` — grid responsivo de cards (`grid-cols-1 md:grid-cols-2 xl:grid-cols-3`)
- `apps/web/modules/saas/agents/components/NewAgentForm.tsx` — form MVP client com react-hook-form + zod resolver + Select agrupado por provider

**Modificados (3 arquivos):**
- `apps/web/app/(saas)/app/(organizations)/[organizationSlug]/agents/page.tsx` — render condicional: `AgentsList` ou `ComingSoon` + botão "Criar agente"
- `apps/web/app/(saas)/app/(organizations)/[organizationSlug]/agents/new/page.tsx` — substitui `ComingSoon` por `NewAgentForm`
- `packages/ai/src/mastra/index.ts` — re-export dos helpers do `models.ts`

## Subtasks (@dev)

- [x] Criar `lib/server.ts` com `getAgentsByOrg` e `getAgentById` usando `cache()` do React
- [x] Criar `lib/schemas.ts` com zod pra `createAgentInput` etc
- [x] Criar `lib/actions.ts` com server actions + revalidatePath
- [x] Criar `SUPPORTED_MODELS` — ficou em `packages/ai/src/mastra/models.ts` com subpath export `@repo/ai/models` pra uso browser-safe
- [x] Criar `AgentsList` + `AgentCard` + `AgentStatusBadge` + `AgentCardActions`
- [x] Criar `NewAgentForm` (react-hook-form + zod resolver)
- [x] Substituir os dois `ComingSoon` nas pages existentes
- [ ] ~~Adicionar chaves pt-BR no JSON~~ — **adiado pra 07B.2** (faz mais sentido adicionar toda a estrutura `agents.*` de uma vez quando criar o shell do detalhe; textos atuais hardcoded em PT-BR)
- [ ] Testar fluxo no browser — **Quality gate humano do Vinni** (esta subtask é dele, não minha)
- [x] `pnpm -F web type-check` passa
- [x] `pnpm -F web lint` passa pros meus arquivos (erros pré-existentes no boilerplate não foram introduzidos por esta story)
- [x] `pnpm -F web build` passa (client bundle não puxa `pg`/Mastra)
- [ ] Atualizar checkpoint — fora de escopo desta story (projeto legacy, sem `projects/{id}/PROJECT-CHECKPOINT.md`) ao concluir

## Definition of Done

- [x] Criar agente pela UI funciona (estrutura pronta — validação em tempo real no gate humano)
- [x] Lista mostra agentes criados com dados corretos
- [x] Dropdown de ações executa cada operação com sucesso
- [x] Empty state aparece quando org não tem agentes
- [x] `pnpm -F web type-check` passa
- [x] `pnpm -F web lint` passa pros meus arquivos
- [x] `pnpm -F web build` passa
- [ ] **Quality gate humano:** Vinni abre `/agents`, clica "+ Novo agente", cria um agente de teste, volta pra lista, usa dropdown (ativar, duplicar, arquivar). Aprova visualmente.

## Notes

- **Zod resolver:** `@hookform/resolvers` já está instalado (usado em outros forms do boilerplate).
- **Imports browser-safe:** precisou criar `packages/ai/models.ts` como subpath export porque importar de `@repo/ai` direto num client component puxava `@mastra/pg` e `pg` pro bundle do browser (falhava no build). Solução: `@repo/ai/models` é o subpath browser-safe. Documentar pras próximas stories que forem consumir `SUPPORTED_MODELS` no client.
- **i18n adiado:** como todos os textos são hardcoded em PT-BR e 100% do produto é PT-BR por ora, deixei sem i18n key. A story 07B.2 vai adicionar a árvore `agents.*` completa de uma vez (tabs + dirty banner + actions) quando a estrutura estiver mais clara.
- **Divergência do spec detectada pro 07B.4 (não bloqueia esta story):** o schema `AgentPersonality` em `packages/database/drizzle/schema/agents.ts` usa enums de strings (ex: `tone: 'formal' | 'semiformal' | 'informal' | 'descontraido'`), mas o UI spec da Sati propõe **sliders 0-100**. Tem conflito. Opções pra resolver na 07B.4: (a) usar radios/select de 4 opções no lugar dos sliders, (b) alterar schema pra `integer` 0-100 (migration aditiva), (c) manter UI slider mas mapear 0-100 pras 4 strings no save. **Decisão pra quando 07B.4 começar.**
- **RLS:** dev já tem member role `owner` no seed. Teste manual com role `member` fica pra 07C se necessário.
- **Tests unitários:** não adicionados nesta story. Padrão seguido da 07A (tests só em helpers críticos). Actions têm happy-path validation via zod parse + guards.
