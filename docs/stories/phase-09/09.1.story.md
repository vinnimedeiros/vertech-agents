---
id: 09.1
title: "Tela de boas-vindas /agents (Hero + grid 7 templates + SessionHistory)"
status: Ready for Review
type: story
project: vertech-agents
epic: "Phase 09"
priority: P0
agent: "@dev (Neo)"
branch: feature/phase-09-architect-ui
tags:
  - project/vertech-agents
  - phase/09
  - story
---

# Story 09.1 — Tela de boas-vindas `/agents` (Hero + grid 7 templates + SessionHistory)

**Status:** Ready for Review
**Phase:** 09
**Owner agent:** `@dev` (Neo)
**Story type:** UI + server components
**Estimated effort:** 3-4h

## Context

Substitui a tela atual `/agents` (que era "Lista simples" na v1) pela nova tela de boas-vindas do Arquiteto. Dois estados: (a) vazio = hero dominante + grid de templates; (b) com agentes = hero comprimido + accordion "Rascunhos" + "Agentes criados".

Esta é a porta de entrada PRIMÁRIA da experiência de criação (PRD v2 + UI Spec § 3).

**Specs de referência:**
- UI Spec § 3 (wireframes desktop + mobile): `docs/phase-09/ui-spec-arquiteto.md`
- Tech Spec § 9.2 (packages afetados): `docs/phase-09/tech-spec-arquiteto.md`

## Acceptance Criteria

### Layout base e estados

1. GIVEN rota `/app/[orgSlug]/agents`, WHEN user sem agentes e sem rascunhos, THEN mostra layout "estado vazio": hero central max-w 640px + CTA "Criar com o Arquiteto" + divisor + grid 4+3 templates.
2. GIVEN user com agentes/rascunhos, WHEN renderiza, THEN layout "estado com agentes": PageHeader comprimido + Accordion "Rascunhos em andamento (N)" expandido + Accordion "Agentes publicados (M)" expandido + Accordion colapsado "Começar do zero com um template".
3. GIVEN breakpoint `lg:` (>=1024px), WHEN desktop, THEN grid de templates 4 cols. GIVEN `md:` (768-1023), THEN 3 cols. GIVEN mobile (<768px), THEN 2 cols + Personalizado na última linha sozinho.

### Hero

4. GIVEN estado vazio, WHEN renderiza hero, THEN título "Crie seu primeiro agente comercial em 15 minutos" (text-4xl font-semibold 2 linhas), subtítulo muted max-w 480px, CTA `Button size="lg"` com `Sparkles` icon + "Criar com o Arquiteto".
5. GIVEN estado com agentes, WHEN hero comprimido, THEN PageHeader padrão com título "Agentes" + subtítulo + CTA à direita "+ Criar novo agente".

### Grid de 7 templates

6. GIVEN grid de templates, WHEN renderiza, THEN 7 cards: Clínica 🏥, E-commerce 🛒, Imobiliária 🏠, Infoprodutor 💼, SaaS ⚙️, Serviços locais 🍽️, Personalizado ✨ (último com `border-dashed`).
7. GIVEN card template, WHEN renderizado, THEN estrutura: ícone 48px centralizado + label (text-base font-medium) + micro-copy 2 linhas text-xs muted.
8. GIVEN hover no card, WHEN mouse over, THEN `border-primary/50` + `scale-[1.02]` com transição 150ms.
9. GIVEN click no card, WHEN user seleciona, THEN navega pra `/app/[orgSlug]/agents/new?template={templateId}`.
10. GIVEN card Personalizado clicado, WHEN navega, THEN usa `templateId=custom`.

### Seção Rascunhos em andamento (Accordion)

11. GIVEN user com sessões DRAFT, WHEN accordion expandido, THEN lista cards horizontais com: avatar, nome do agente/template, "Etapa {stage} • há {tempo}", CTA "Continuar".
12. GIVEN card de rascunho clicado (nome ou CTA), WHEN navega, THEN vai pra `/agents/new?session={sessionId}`.
13. GIVEN sessão com `draftSnapshot.lastActivity` há 3h, WHEN renderiza, THEN mostra "há 3 horas". (Usar helper de formatação de tempo relativo existente ou criar.)
14. GIVEN 0 rascunhos, WHEN accordion, THEN mostra mensagem "Nenhum rascunho em andamento".

### Seção Agentes publicados (Accordion)

15. GIVEN user com agentes ACTIVE/DRAFT/PAUSED (não ARCHIVED), WHEN accordion expandido, THEN grid de cards reusando `AgentCard` da 07B.1 v1 (compatibilidade preservada).
16. GIVEN card agente clicado, WHEN navega, THEN vai pra `/agents/[agentId]` (Painel de Refino, que será implementado em 07B-v2 mas pode ter placeholder agora — fallback OK).

### Seção Templates (accordion colapsado)

17. GIVEN user com agentes, WHEN accordion "Começar do zero com um template" colapsado, THEN usuário pode expandir pra ver o grid de 7 templates (mesmo componente da estado vazio).

### Server logic

18. GIVEN rota é server component, WHEN renderiza, THEN carrega via `getAgentsByOrg(orgId)` (já existe 07B.1 v1), `getDraftSessions(userId, orgId)` NEW.
19. GIVEN `getDraftSessions`, WHEN query, THEN filtra `agent_creation_session` WHERE `userId = $1 AND organizationId = $2 AND status = 'DRAFT'`, ordena `updatedAt DESC`, limita 6 (UI mostra ver-todos se passar).

### Translations

20. GIVEN textos novos, WHEN i18n, THEN adicionar namespace `architect.welcome.*` em `pt-BR.json` (ver UI Spec § 14 chaves exatas).

## Scope IN

- Layout responsivo desktop + tablet + mobile
- Hero dinâmico (2 estados)
- Grid de 7 templates com hover states
- Accordion Rascunhos + Agentes + Templates
- Server queries novas (`getDraftSessions`)
- Translations pt-BR namespace `architect.welcome`

## Scope OUT

- Chat do Arquiteto (`/agents/new`) — fica na 09.2
- Detalhe do agente (Painel de Refino) — fica em 07B-v2
- Quick actions no rascunho ("descartar") — adiado

## Dependencies

- **Blocked by:** 07B.1 v1 code (AgentCard, getAgentsByOrg reusados)
- **Blocks:** 09.2 (chat carrega com `?template={id}` passado daqui)

## File List (esperado)

Novos:
- `apps/web/modules/saas/agents/architect/components/welcome/Hero.tsx`
- `apps/web/modules/saas/agents/architect/components/welcome/TemplateGrid.tsx`
- `apps/web/modules/saas/agents/architect/components/welcome/TemplateCard.tsx`
- `apps/web/modules/saas/agents/architect/components/welcome/SessionHistory.tsx`
- `apps/web/modules/saas/agents/architect/components/welcome/DraftCard.tsx`
- `apps/web/modules/saas/agents/architect/lib/templates.ts` (registry dos 7 templates: id, label, emoji, description)
- `apps/web/modules/saas/agents/architect/lib/server.ts` (getDraftSessions)
- `apps/web/modules/saas/agents/architect/lib/time-format.ts` (formatRelativeTime, se não existe helper global)

Modificados:
- `apps/web/app/(saas)/app/(organizations)/[organizationSlug]/agents/page.tsx` (troca conteúdo)
- `apps/web/modules/i18n/translations/pt-BR.json` (adicionar `architect.welcome.*`)

## Subtasks (@dev)

- [x] Criar `templates.ts` com 7 entries (inclui Personalizado)
- [x] Criar TemplateCard + TemplateGrid responsivo
- [x] Criar Hero com 2 variantes (empty vs comprimido)
- [x] Criar SessionHistory (accordion + DraftCard)
- [x] Criar server.ts com `getDraftSessions`
- [x] Integrar tudo em `/agents/page.tsx`
- [ ] ~~Adicionar translations pt-BR~~ — **divergência documentada**: projeto usa pt-BR hardcoded nos componentes, não next-intl. Seguido pattern existente (AgentCard, AgentsList etc)
- [ ] Responsividade mobile testada — **fica pro gate humano em dev** (sem harness E2E aqui)
- [x] Typecheck + lint passam (build não roda localmente — depende de env vars, typecheck cobre compilation)
- [ ] Quality gate humano Vinni (testar tela em dev, ver 2 estados)

## Definition of Done

- [ ] Estado vazio: hero + grid 7 templates renderiza
- [ ] Estado com agentes: accordion + cards
- [ ] Mobile 2 colunas com Personalizado destacado
- [ ] Hover/click em template navega pra /agents/new?template=
- [ ] Click em rascunho navega pra /agents/new?session=
- [ ] Click em agente publicado navega pra /agents/[id]
- [ ] Typecheck + lint + build passam
- [ ] Quality gate humano Vinni

## Notes

- Como `/agents/new` ainda não existe nesta story (vem 09.2), a navegação pra ela mostra 404 ou placeholder temporário. OK por ora, 09.2 fecha.
- Ícones dos templates são emojis (UI Spec Dilema decidido). Se @dev preferir Lucide, alinhar com Sati antes.
- Cuidado com reuso de `AgentCard` da 07B v1 pra não introduzir regressão em `/agents` existente (que já renderiza cards daquele componente na branch de hold).

## Dev Agent Record

### Agent Model Used

Claude Opus 4.7 (1M context) — `@dev` (Neo), pattern aprovado pelo Vinni (dispensa `@po` conforme 07A/07B/08A)

### Completion Notes List

- **Branch nova `feature/phase-09-architect-ui`** criada a partir de `feature/phase-08a-09-architect` pra não contaminar escopo do PR #1 (08-alpha).
- **Estrutura criada:** `apps/web/modules/saas/agents/architect/{components/welcome,lib}`. Separado do modulo `agents/` tradicional (07B v1) pra deixar claro que é o novo fluxo Arquiteto.
- **Dois estados automáticos** baseados em `agents.length === 0 && drafts.length === 0`:
  - **Vazio:** Hero grande + Separator + grid de 7 templates
  - **Com agentes:** Hero comprimido + SessionHistory (accordion) + Agentes publicados (accordion) + Templates (accordion colapsado)
- **7 templates** em `templates.ts`: clinical, ecommerce, real_estate, info_product, saas, local_services, custom (dashed). Registry exporta `findArchitectTemplate(id)` pra routes futuras de `/agents/new?template={id}` (story 09.2).
- **TemplateCard** com hover `scale-[1.02]` + `border-primary/50`, `border-dashed` pro custom.
- **TemplateGrid** responsivo via Tailwind: `grid-cols-2 md:grid-cols-3 lg:grid-cols-4`. Mobile fica com 3 linhas de 2 + 1 Personalizado alinhado à esquerda na 4ª linha (natural do CSS grid).
- **SessionHistory** usa shadcn Accordion `type="single" collapsible`. Defaults aberto quando há drafts. Empty state inline.
- **DraftCard** horizontal com avatar de emoji do template + título derivado de `draftSnapshot.agentName || businessName || templateLabel || template.label` + "Etapa {stage} · há {tempo}" + CTA "Continuar".
- **Hero** com duas variantes via prop `variant="empty" | "compressed"`. Variante empty é max-w 640px centralizada com CTA `size="lg"`; compressed é header padrão com CTA à direita.
- **Server query `getDraftSessions(userId, orgId)`** filtra `status = 'DRAFT'`, ordena `updatedAt DESC`, limita 6. Tipo `DraftSessionRow` exportado pro DraftCard consumir.
- **Helper `formatRelativeTime`** próprio em pt-BR ("há 5 minutos", "há 3 horas", "há 2 dias", "há 1 semana", "há 2 meses", "há 1 ano"). Sem dep externa — calculo inline via Date.now.
- **Divergência da spec (AC20):** AC original pede adicionar translations em `pt-BR.json`. Projeto **usa pt-BR hardcoded** nos componentes (ver AgentCard, AgentsList, page.tsx antiga). Seguido pattern existente — translations não adicionadas. Se Vinni quiser i18n de verdade, é refactor cross-projeto pra outra story dedicada.
- **Testes manuais recomendados (gate humano):**
  - `pnpm dev` em org nova (sem agentes): deve mostrar Hero empty + grid de 7 templates centralizado
  - Criar 1 `agent_creation_session` DRAFT manualmente no banco (ou deixar 09.2 fazer quando estiver pronto): deve mostrar Hero comprimido + accordion Rascunhos com 1 item + Agentes vazio accordion + Templates colapsado
  - Resize browser pra testar mobile (2 cols) / tablet (3 cols) / desktop (4 cols)
  - Click em template navega pra `/agents/new?template={id}` (404 temporário até 09.2)
  - Click em rascunho navega pra `/agents/new?session={id}`

### File List

**Novos (8):**
- `apps/web/modules/saas/agents/architect/components/welcome/Hero.tsx`
- `apps/web/modules/saas/agents/architect/components/welcome/TemplateCard.tsx`
- `apps/web/modules/saas/agents/architect/components/welcome/TemplateGrid.tsx`
- `apps/web/modules/saas/agents/architect/components/welcome/DraftCard.tsx`
- `apps/web/modules/saas/agents/architect/components/welcome/SessionHistory.tsx`
- `apps/web/modules/saas/agents/architect/lib/templates.ts`
- `apps/web/modules/saas/agents/architect/lib/time-format.ts`
- `apps/web/modules/saas/agents/architect/lib/server.ts`

**Modificados (1):**
- `apps/web/app/(saas)/app/(organizations)/[organizationSlug]/agents/page.tsx` — substituído layout ComingSoon + AgentsList por Hero + SessionHistory + Accordions

### Debug Log References

Validações rodadas com zero erros:
- `pnpm -F web type-check` — PASS (sem erros novos)
- `pnpm exec vitest run` — 92/92 monorepo PASS
- `pnpm exec biome check` nos arquivos novos — 0 errors, 4 warnings `useBlockStatements` (pattern pré-existente)

## Change Log

| Data | Autor | Mudança |
|---|---|---|
| 2026-04-19 | @sm Niobe | Story criada como Draft |
| 2026-04-20 | @dev Neo | Implementação completa. 8 arquivos novos + 1 modificado. Dois estados dinâmicos. Typecheck + tests verdes. Status: Ready for Review |
